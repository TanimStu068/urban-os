import 'package:flutter/material.dart';

class MetricItem {
  final String label, value;
  final Color color;
  final IconData icon;
  const MetricItem(this.label, this.value, this.color, this.icon);
}
